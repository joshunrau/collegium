---
title: Environment
description: The environment variables the app reads.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- `APP_HOST` and `APP_PORT`: where the callback endpoints bind, and why that address must not be
  publicly routable
- `CONFIG_PATH`: where the app finds `config.json`
- `DATABASE_URL`: the SQLite database that holds conversation state

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
