---
title: HTTP Endpoints
description: The callback endpoints and the network-trust posture.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- `POST /triggers`, `POST /decisions`, and `POST /commands`: payloads and refusals
- Unauthenticated by decision: what that assumes, and how to bind accordingly
- What intake refuses loudly rather than recording

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
