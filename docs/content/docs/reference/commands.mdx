---
title: Commands
description: The slash-command surface an operator drives from chat.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- The full surface: `/trace`, `/forget`, `/reset`, `/stop`, `/kill`, `/resume`, `/queue`,
  `/triggers`, and memory review
- Which commands respond ephemerally and which post to the channel
- Who may run them: anyone present, because stopping is always safe
- How the framework registers and reconciles its own commands at boot

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
