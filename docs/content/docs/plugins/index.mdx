---
title: Plugins
description: Operator-supplied capability — the declarePlugin contract and the trust model.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- What a plugin may contribute — tools, skills, durable records — and why that is the whole surface
- Qualified names and per-agent grants: installing a plugin grants it to no one
- Declared, never discovered: every plugin failure is a startup failure
- Plugins are fully trusted; agents are not
- `@collegium/sdk` as the sole import, and what that boundary buys both sides

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
