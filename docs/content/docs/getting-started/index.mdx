---
title: Quickstart
description: From docker compose up to a running workspace.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- What `docker compose up` brings up: the app and a bundled Mattermost server
- The environment the app reads: `APP_HOST`, `APP_PORT`, `CONFIG_PATH`, `DATABASE_URL`
- What boot checks — and what it refuses to start without
- Logging into the workspace and finding the system bot

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
