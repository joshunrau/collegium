---
title: Giving an Agent a Mailbox
description: Exchange or IMAP, the inbound poll, and what a send approval shows.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- One agent, one address, fixed in configuration
- Exchange Online and IMAP/SMTP behind one seam
- Inbound: the poll, deduplication, and a backlog that drains at a bounded rate
- Reading ungated, sending gated — and no bcc anywhere
- Why a send is never retried

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
