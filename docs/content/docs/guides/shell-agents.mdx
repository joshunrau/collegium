---
title: Granting Shell Access
description: Dedicated OS users, sudo, and what confinement actually enforces.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- Why shell is a per-agent grant, not a default
- Provisioning the dedicated OS user, and what boot probes before it will start
- What OS permissions enforce that tool code never could
- Every command approved: presented inline, in full, or refused as too long to show

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
