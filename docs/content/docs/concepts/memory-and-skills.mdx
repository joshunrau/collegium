---
title: Memory & Skills
description: What an agent retains between conversations, and the procedures it can load on demand.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- Memory entries: descriptions in every prompt, bodies loaded on demand
- Why memory writes are ungated — and the disclosure line that compensates
- Caps, eviction, and provenance
- Memory as the one path by which information crosses channels
- The skill manifest and `load_skill`; why agents cannot write skills

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
