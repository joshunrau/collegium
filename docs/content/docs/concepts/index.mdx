---
title: How Collegium Works
description: The whole model in five minutes — chat as the substrate, turns, approvals, and work you can watch.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- Agents as persistent colleagues: bot users in your workspace, each with its own prompt, model,
  tools, skills, and memory
- The turn: the unit of execution, started only by a post — never by the agent itself
- The approval gate: what blocks for consent, and what is merely disclosed
- Where work comes from: humans in channels and DMs, and triggers for mail, cron, and webhooks
- What you see: the status post, the 👀 acknowledgement, and the full trace behind `/trace`
- Where to go deeper: the rest of this section, then the [specification](/docs/specification)

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
