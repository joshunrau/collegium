---
title: Unattended Work
description: Triggers, mail intake, and idle gating — how work nobody typed reaches an agent.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- The trigger table: deterministic sources write rows, the system bot announces them
- Idle gating: why a trigger is only ever posted into a quiet channel
- Mail intake: the poll, the dedupe key, and a backlog that drains at a bounded rate
- Reading is ungated, sending is gated — and what a send approval shows
- Why none of this violates the rule that chat is the only substrate

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
