---
title: Approvals
description: The gate — full payloads, denial semantics, and the action budget.
---

<Callout type="warn" title="Not written yet">
  This page is a placeholder. The site scaffold is in place; the prose is not.
</Callout>

Planned coverage:

- Gated, ungated, and dynamic tools — and why reads stay free
- The approval prompt: the full payload, never a summary
- Approve, deny, and deny-with-reason — and why a bare denial ends the turn
- Who may approve: presence in the channel confers authority
- The ten-attempt action budget, extensions, and what a denied extension leaves the agent
- Approval verifies the call, not the content — the known limitation, stated plainly

Until then, the [specification](/docs/specification) is the authoritative description of how the
system behaves.
