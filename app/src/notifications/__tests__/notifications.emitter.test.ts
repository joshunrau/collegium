import { describe, expect, it } from 'vitest';

import { NotificationsEmitter } from '../notifications.emitter.ts';

describe('NotificationsEmitter', () => {
  it('should be defined', () => {
    expect(NotificationsEmitter).toBeDefined();
  });
});
