/** the one bare specifier a plugin may import; the compiler rewrites it to the framework's own copy */
export const SDK_SPECIFIER = '@collegium/sdk';

/** skill documents are assets of the module, so they resolve beside the entry, not the package root */
export const SKILLS_DIRECTORY = 'skills';
