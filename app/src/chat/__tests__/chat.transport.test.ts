import { describe, expect, it } from 'vitest';

import { ChatTransport } from '../chat.transport.ts';

describe('ChatTransport', () => {
  it('should be defined', () => {
    expect(ChatTransport).toBeDefined();
  });
});
