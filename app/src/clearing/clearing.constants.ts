/** the path beneath APP_PUBLIC_URL the confirmation dialog submits to */
export const CLEARING_PATH = '/clearing/confirm';

/** how long a confirmation dialog's submission is honoured after the command opened it */
export const CONFIRMATION_TTL_MS = 10 * 60 * 1000;
