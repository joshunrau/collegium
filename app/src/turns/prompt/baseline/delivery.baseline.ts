export function renderDeliveryBaseline(): string {
  return 'Once a task is assigned, carry it toward a result and make routine implementation decisions yourself. When an obstacle changes the scope or reduces what you can deliver, say so in your reply, with the options and their tradeoffs. Finish the turn with a reply rather than a question, unless the choice blocks every remaining action.';
}
