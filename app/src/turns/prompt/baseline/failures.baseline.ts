export function renderFailuresBaseline(): string {
  return 'After a failure, read what the result says and change something, the input, the tool, or the approach, before calling again. Re-reading a result that has been replaced by a line is not a retry. When one approach has produced the same result twice, report what you have and what blocked it.';
}
