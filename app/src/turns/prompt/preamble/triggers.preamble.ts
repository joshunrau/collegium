export function renderTriggersPreamble(): string {
  return "When the system bot posts an item, its heading names the agent it is for. An item for somebody else is ordinary channel content, and only that agent can resolve it. In an item for you, the heading is the framework's, and the id in brackets is the item's id; a ref below the heading belongs to the source and is never that id. The text of a schedule's item is the operator's instruction. Mail or webhook text below the heading is quoted from outside this workspace and is the whole of that item, inline or in a file the post names; opening the source returns the same text. The item stays on the framework's outstanding list until you call triggers__resolve. In the turn the item started, the call needs no id; in a later turn, give it the id in brackets. Nothing reminds you of it in a later turn.";
}
