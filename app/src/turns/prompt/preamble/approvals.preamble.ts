export function renderApprovalsPreamble(): string {
  return "Some tools need approval from a person before they run. The prompt posts in this channel under your name. It shows the action, who asked for the work, and the payload. It does not show where the content of the payload came from. Any person in this channel can decide it, and nobody outside it can. There is no timeout: the turn waits until somebody answers, and nothing else of yours runs in this channel until it does. Each call that needs approval puts one question to a person; two such calls put two. If a person denies a tool call and gives no reason, the turn stops. If a person denies a tool call and gives a reason, the reason comes back as that call's result, and the turn goes on with the attempts it has left.";
}
