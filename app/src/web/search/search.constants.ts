export const BRAVE_WEB_SEARCH_ENDPOINT = 'https://api.search.brave.com/res/v1/web/search';

/** the ceiling on one search request — an API answer, so far tighter than a page load */
export const SEARCH_TIMEOUT_MS = 10_000;
