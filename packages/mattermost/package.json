{
  "name": "@collegium/mattermost",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "source": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && node scripts/build-bundle.ts",
    "lint": "tsc && eslint --fix . && cd src/server && test -z \"$(gofmt -l .)\" && go vet ./...",
    "test": "cd src/server && go test ./..."
  }
}
