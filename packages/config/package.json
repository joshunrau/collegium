{
  "name": "@collegium/config",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "source": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "lint": "tsc && eslint --fix ."
  },
  "dependencies": {
    "@collegium/core": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "type-fest": "catalog:"
  }
}
